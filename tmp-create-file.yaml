- name: Copy using inline content
  hosts: all
  tasks:
  - name: create tmp file
    ansible.builtin.copy:
      content: "# This file on host {{ inventory_hostname }} is generated by ansible"
      dest: "{{ tracked_file }}"
